# discord-entropy

generate random output from a discord channel

usage: ./discord_entropy.sh --token <discord_token> --channel-id <channel_id> [--limit <message_limit>] [--entropy-size <size_in_bytes>]
